GLOBAL_JOB_EXECUTION_ENVIRONMENTS = [
{% for item in _global_job_execution_environments | default(global_job_execution_environments) %}
    {'name': {{ item.name | quote }}, 'image': '{{ item.image }}'},
{% endfor %}
]
CONTROL_PLANE_EXECUTION_ENVIRONMENT = '{{ _control_plane_execution_environment | default(control_plane_execution_environment) }}'
