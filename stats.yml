- name: Test Stats
  hosts: localhost
  connection: local
  tasks:

  - name: Print
    ansible.builtin.debug:
      msg: "{{ stat1 }}"
    ignore_errors: true

  - name: Create an Stat
    ansible.builtin.set_stats:
      data:
        stat1: zaskan

  - name: Print
    ansible.builtin.debug:
      msg: "{{ stat1 }}"