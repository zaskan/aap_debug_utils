# Localhost
local   all             all                                     trust
host    all             all             127.0.0.1/32            md5

# Master server
host    replication     {{ rp_user }}         {{ hostvars[play_hosts[0]].ansible_default_ipv4.address }}/32      md5

# Slave server
host    replication     {{ rp_user }}         {{ hostvars[play_hosts[1]].ansible_default_ipv4.address }}/32         trust
